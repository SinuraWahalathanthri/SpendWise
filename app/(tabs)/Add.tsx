import { View } from "react-native";
import React from "react";

// This is a dummy component required by Expo Router
// The actual functionality is in the TabLayout via the modal
const Add = () => {
  return <View />;
};

export default Add;
